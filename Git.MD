5 git commands

git init
git add .
git remote origin <url>
git commit -m "Message"
git push -u origin main


5 Docker commands

docker images 
docker containers
docker tag username/imagename 
docker push imagename@version
docker pull imagename

5 kubernates commands

kubectl pods 
kubectl 
kubectl deploy 
kubectl 


